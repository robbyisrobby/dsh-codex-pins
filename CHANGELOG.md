# Changelog

## 1.1.1

- Pin accent color is Harness blue (`#3884ff`) instead of amber.

## 1.1.0

- Split the sidebar into a Pinned pane and a Recents pane.
- Pinned sessions no longer also appear in Recents.
- Each pane scrolls on its own.

## 1.0.0

- First release: pin sessions from a row or the conversation header.
- Persist pins in `localStorage`, with a one-time import from `dsh-session-pin`.
